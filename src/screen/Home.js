import {StyleSheet, Text, View} from 'react-native';
import React from 'react';
import BottamStack from '../Navigation/BottamStack';

const Home = () => {
  return (
    <View style={{marginBottom: 0}}>
      <BottamStack />
    </View>
  );
};

export default Home;

const styles = StyleSheet.create({});
